import './fetch.css'
import {useState} from 'react'

function Thread(props){
    return 
    <thread>
        <h2>{props.title}</h2>
        {props.body}
    </thread>
}

function App(){
    return <p>Nothing</p>
}

export default App;